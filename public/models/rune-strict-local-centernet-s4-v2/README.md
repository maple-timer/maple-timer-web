# Rune detector runtime bundle

Copy this directory as one unit. `metadata.json` is the preprocessing, output, decode, and threshold contract. `verify-report.json` records frozen PyTorch/ONNX parity; it is not a training report.
